.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# Personal resume data — real PII, never commit. Use examples/sample_resume.yaml instead.
resume.yaml
*.resume.yaml

# Real build profiles (actual job targeting) — personal, not for the public repo.
# Generic samples live in examples/, which is tracked.
/profiles/

# Real resume PDFs / extracted JSON (rac ingest inputs) — personal data, never commit.
/resume_archive/
*.pdf
!examples/*.pdf
